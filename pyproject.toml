[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "typosquat-guard"
version = "0.1.0"
description = "Detect typosquatting and suspicious packages in dependency files"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name = "Your Name" }
]

[project.scripts]
typosquat-guard = "typosquat_guard.cli:main"
